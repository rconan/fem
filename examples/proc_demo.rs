use fem::io::{IOData, IO};
use fem_proc::ad_hoc;
use serde;
use serde::Deserialize;

ad_hoc!();
fn main() {
    //    println!("{:#?}", std::env::var("FEM_REPO"));
    //    println!("{}", answer());
}
